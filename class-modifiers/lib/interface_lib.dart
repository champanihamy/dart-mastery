/// Outside the library, other developers can see your methods and fields,
/// but they cannot use extends. They must use implements and write their own logic for every single method.

interface class Encryptor {
  void encrypt() => print("Standard encryption logic");
}
